# sandbox

Scratch notebooks — a dictionary-API word scraper, a bimodal / z-score doodle. Nothing here is relied on elsewhere.

Also `Disassembly Sequence Optimization key steps.py` — a genetic-algorithm (NSGA-II) disassembly-sequence optimizer, parked here for now.
